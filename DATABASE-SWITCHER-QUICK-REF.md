# 数据库切换 - 快速参考

## 🎨 视觉识别

### 🟢 开发测试环境 (sstv2) - 安全
```
┌─────────────────────────────────────────┐
│ ✅ 開發測試環境 (sstv2)    [sstv2 ▼]   │  <- 绿色背景
└─────────────────────────────────────────┘
```
→ **可以放心操作，不会影响生产数据**

### 🔴 正式环境 (sst) - 警告！
```
┌─────────────────────────────────────────┐
│ ⚠️ 正式環境 (sst) - 請小心操作！        │  <- 红色背景 + 闪烁
│                           [sst ▼]        │
└─────────────────────────────────────────┘
```
→ **所有操作会直接影响生产数据！**

---

## 🔄 快速切换步骤

### 切换到开发环境 (推荐日常使用)
1. 点击横幅右侧下拉框
2. 选择 "sstv2 (開發測試)"
3. 确认对话框点击"确定"
4. 等待 3-5 秒
5. 重新运行: `.\start-server.ps1`
6. 页面自动刷新 → 看到绿色横幅 ✅

### 切换到生产环境 (谨慎！)
1. 点击横幅右侧下拉框
2. 选择 "sst (正式環境 ⚠️)"
3. **仔细阅读警告对话框**
4. 确认理解风险后点击"确定"
5. 等待 3-5 秒
6. 重新运行: `.\start-server.ps1`
7. 页面自动刷新 → 看到红色闪烁横幅 🔴

---

## ⚡ 启动命令

```powershell
# 全部启动 (推荐)
.\start-all-apps.ps1

# 仅后端
.\start-server.ps1

# 仅前端
.\start-web.ps1
```

---

## 🆘 问题处理

### Q: 切换后页面一直转圈
**A**: 后端未重启，执行: `.\start-server.ps1`

### Q: 不知道当前用的哪个数据库
**A**: 看左侧导航栏顶部横幅颜色
- 🟢 绿色 = sstv2 (开发)
- 🔴 红色 = sst (生产)

### Q: 切换后横幅颜色没变
**A**: 按 Ctrl+Shift+R 强制刷新浏览器

### Q: 想确认数据库配置
**A**: 
```powershell
# 查看配置文件
Get-Content src\SST.StockImport.API\appsettings.json | Select-String "Database="
```

---

## 🔒 安全提示

1. **日常开发请使用 sstv2** (绿色)
2. **仅在必要时切换到 sst** (红色)
3. **看到红色横幅时加倍小心**
4. **不确定时，看横幅颜色！**

---

## 📞 紧急联系

如果误操作生产数据：
1. 立即切换回 sstv2
2. 检查 `D:\DBbackup\OWN\` 的最新备份
3. 使用"資料庫備份還原"功能恢复

---

**记住**: 
- 🟢 绿色 = 安全 = sstv2
- 🔴 红色 = 警告 = sst (生产)
