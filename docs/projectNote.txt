1. 原系統是好幾年來陸續寫的系統
寫的時候也不太在乎維護的問題，只在乎解決眼前的問題
所以呢？有些程式就變得非常非常的大
你在參考這些程式的時候，基本原則是，不可以一次把原始碼讀進去
是要看哪個部分就用search到那個方向再讀進去
不然三兩下就把你的Contact塞爆

尤其是 goodinfo， 


2. 測試金字塔 
單元測試 -> 無伺服器整合測試 -> 有伺服器的整合測試 (webapi) -> 出 Sandbox 測試 (單元測試 -> 無伺服器整合測試 -> 有伺服器的整合測試) -> 整合前台測試 -> UAT
前一层是后一层的基础，层层相依 複雜漸增, 確保成功


3. 本系統
標準 Port ： 5008
標準脚本 :
全部:
cd D:\vibeCoding\sst
.\start-all.ps1
 
單獨 server： 
cd D:\vibeCoding\sst
.\start-server.ps1

單獨 前臺
  cd D:\vibeCoding\sst  
  .\start-web.ps1
  
  http://localhost:5089/
  
开收工提示: 
=========================
## 开工:
1. 讀取 [copilot-instructions.md](http://copilot-instructions.md/)
2. 專案注意事項: Docs/projectNote.txt
2. 從 git 讀取交接文件, 確認待辦事項


## 收工 Checklist
請確認無縫交接 待辦事項 特別重要
请依照 Docs\SESSION_CHECKLIST.md
 Phase 1: 文件先行（完整版）
 Phase 2: 代碼品質
 Phase 3: 依賴與整潔
	  錯的/舊的過期程式就直接刪除
	  不要留在專案， 造成未來的混淆 
 Phase 4: Git 提交


 Set-Location d:\vibeCoding\sst\src\GoodInfoTestWeb; dotnet run --no-build
 
 cd d:\vibeCoding\sst\tests\GoodInfo19LinksTest; Get-ChildItem run*.ps1; Start-Sleep -Seconds 1; powershell -NoProfile -ExecutionPolicy Bypass -File "run-19-tests.ps1"


